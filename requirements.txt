# Core AI & Graph
h2ogpte==1.6.55
neo4j>=5.18.0
python-dotenv>=1.0.0

# Data loading
pandas>=2.2.0

# Layer 2 — regulatory pipeline
pdfplumber>=0.11.0
pyyaml>=6.0.0

# App
streamlit>=1.35.0
plotly>=5.22.0

# Notebook
jupyter>=1.1.0
jupyterlab>=4.0.0
ipykernel>=6.29.0
ipywidgets>=8.0.0

# 31x Investigation system — MCP
fastmcp>=0.1.0          # FastMCP server framework (jlowin/fastmcp)
mcp-neo4j-cypher        # Neo4j Labs MCP server (neo4j-contrib/mcp-neo4j)
mcp                     # MCP Python client

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0
